# 현재 Symposium 은 별도의 환경변수 시크릿이 필요 없습니다.
# LLM 응답은 로컬 `claude` CLI(구독 요금제, --print)로 처리하며 API 키를 쓰지 않습니다.
# (레거시 anthropic API 경로 generate.py 및 ANTHROPIC_API_KEY 는 v0.13.0 에서 제거되었습니다.)
#
# 향후 시크릿이 필요해지면 이 파일에 규약을 추가하세요. .env 는 .gitignore 로 커밋되지 않습니다.
